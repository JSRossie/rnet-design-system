# Font build toolchain — recreate with fonts/build-fonts.sh setup line
fonts/.venv/
# Env
.env

# Wrangler build cache
.wrangler/

# macOS
.DS_Store

# Scratch / exploratory samples — not part of the system
.temp/